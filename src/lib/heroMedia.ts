/**
 * Substituir estes valores quando o vídeo cinematográfico (Porsche) for definido.
 * HERO_VIDEO_SRC: vídeo principal (desktop/tablet).
 * HERO_VIDEO_MOBILE_SRC: opcional, versão mais leve/recortada para mobile. Se null, usa HERO_VIDEO_SRC.
 * HERO_POSTER_SRC: frame estático exibido antes do vídeo carregar e em conexões lentas (desktop).
 * HERO_MOBILE_POSTER_SRC: opcional, poster equivalente pro mobile. Se null, usa HERO_POSTER_SRC.
 * Enquanto nenhum vídeo existir, o Hero usa um fundo em gradiente escuro no lugar.
 */
export const HERO_VIDEO_SRC: string | null = null;
export const HERO_VIDEO_MOBILE_SRC: string | null = null;
export const HERO_POSTER_SRC: string | null = null;
export const HERO_MOBILE_POSTER_SRC: string | null = null;
